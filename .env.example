# Copia este archivo a .env y completa tus propios valores.
# NUNCA subas el archivo .env real (ya esta en .gitignore).

# Claude API (para OCR/extraccion de diagnostico de la hoja de referencia)
ANTHROPIC_API_KEY=

# Opcional: si usas algun servicio de geocoding
# GEOCODING_API_KEY=
